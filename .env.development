# Act 0 to 2 only. Compiled into the bundle by Vite, which is exactly the
# problem: the value that reaches production was never the value you tested.
# Act 3 deletes all three of these files.
VITE_API_BASE=https://certcheck-api-dev.certcheck.workers.dev
